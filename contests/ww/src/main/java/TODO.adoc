= TODO LIST
* Revoir l'eval : il faut une eval sur l'etat, pas sur l'action
		points marqués
		hauteur (fibonnacci)
		hauteurs autour
		actions possibles
		cases reachables
		position sur le board (relative à l'autre, aux ennemis ou au centre)
		
  
= TO VALIDATE
* Si une unitÃ© est bloquÃ©e dÃ©finitivement, il faut la locker pour ne pas oublier sa pos dans le fog of war
KO(temporaire) : y'a des cas pas géré

* simu Minimax : En fin de simulation, faire un round supplÃ©mentaire avec push dans chaque dir possible, build dans chaque dir possible
  Eval et prendre le plus mauvais score (comme un minimax, mais sur un etage)
  Si on connait la position des 2 unitÃ©s, il faut filtrer les actions sur celles qui seront possible :)
KO ==> ca rend l'agent trop frileux ou alors c'est bourré de bug ;)  

= DONE
